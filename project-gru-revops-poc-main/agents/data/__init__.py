"""Mock data and RevOps query layer.

- "crm" and "pillar": hardcoded account book.
- "dummy_store": JSON tables under dummy_data/.
- "revops", "query", "ops": scope, filters, and table calculators.
- "runtime": run a specialist query without the LLM (debug or fallback).
"""
